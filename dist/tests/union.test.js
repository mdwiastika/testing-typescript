"use strict";
describe("Union Type", () => {
    it("Should be contain union type", () => {
        let sample = "Marcel";
        console.info(sample);
        sample = 18;
        console.info(sample);
        sample = true;
        console.info(sample);
        sample = [10, 20];
    });
});
