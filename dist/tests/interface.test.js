describe("Interface Typescript", () => {
    it("should be support interface", () => {
        const seller = {
            id: 2,
            name: "Budi",
        };
        console.info(seller);
    });
});
export {};
